/** @format */

import React from 'react';

function SidebarFooter(props) {
	return (
        <footer>
            {/* Link back to the top of the section */}
        </footer>
		);
}

export default SidebarFooter;